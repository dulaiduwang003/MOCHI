<div align="center">

# Star · セットアップ & デプロイガイド

[← README へ戻る](README.ja.md)

[简体中文](GUIDE.md) · [English](GUIDE.en.md) · **日本語**

</div>

---

## 1. インストールと実行

**環境**：Windows 10 / 11 · Python ≥ 3.11 · [uv](https://github.com/astral-sh/uv)

```powershell
# 依存関係をインストール
uv sync

# 実行
uv run python main.py
```

初回起動時にコントロールパネルが表示されるので、お使いの大規模言語モデルの設定を入力してください。

- **API Key** / **インターフェースアドレス（base_url）** —— 任意の **OpenAI 互換**サービス（通義千問 / DeepSeek / OpenAI / ローカル vLLM、Ollama……）
- **対話モデル** —— メインのやり取りに使う chat モデル
- **埋め込みモデル** —— ナレッジベースと記憶の意味検索に使用（埋め込みエンドポイントがなければ自動で部分文字列検索にフォールバックするため、未入力でも可）
- **返答言語 / 能力スイッチ / 自発頻度 / 発散度と最大長 / 思考過程を表示するか** —— いずれもパネル内で調整でき、各ターンでリアルタイムに反映される

設定が済むと、Star は画面の隅から「入場」してきます。クリック（または `Ctrl + Alt + S`）で文字を打ってチャット。タスクトレイのアイコンからいつでもパネルを再表示できます。

> 設定はローカルの `data/settings.json` にのみ保存され、どこにもアップロードされません。データディレクトリは環境変数 `STAR_DATA_DIR` で上書きできます。

---

## 2. パッケージ配布（Windows · PyInstaller）

Star を Python 環境に依存しない、ダブルクリックで使える Windows プログラムにパッケージングします。

### 2.1 ワンクリックパッケージ

```powershell
.\build.ps1
```

これは手動で以下を実行するのと等価です。

```powershell
uv sync                                   # 依存関係をインストール（dev グループの pyinstaller を含む）
uv run pyinstaller star.spec --noconfirm
```

成果物：**`dist\Star\Star.exe`** —— 配布の際は**`dist\Star\` ディレクトリ全体**を一緒に相手へコピーしてください（exe の隣にあるものの山が、その依存関係です）。

### 2.2 主要な設計

| 項目 | 選択 | 理由 |
| --- | --- | --- |
| 形態 | **onedir**（ディレクトリ版） | 起動が速い。RapidOCR のモデルは比較的大きく、単一ファイル版は毎回の解凍が非常に遅くなる |
| コンソール | **なし**（`console=False`） | ダブルクリックで黒い窓が出ない |
| データディレクトリ | **`%APPDATA%\Star`** | パッケージ後の exe の隣は読み取り専用 / 書き込み権限なし。設定、記憶、ログはすべてここに書く |

開発時（`uv run python main.py`）は引き続きプロジェクト内の `data\` を使い、パッケージ版は `%APPDATA%\Star` を使う。どちらも `STAR_DATA_DIR` で上書き可能。

### 2.3 独立した Python を内蔵（run_python を使えるようにする）

パッケージ後、`sys.executable` は Python ではなく `Star.exe` になるため、それで直接 `run_python` を走らせると Star 自身が起動してしまう。そこで `build.ps1` は追加で**公式の embeddable Python + pip** をダウンロードし、`dist\Star\pyruntime\` に配置する。`run_python` / `install_package` はパッケージ版（`frozen` 時のみ）で自動的にこれを使うよう切り替わり、開発時には影響しない。

- pyruntime は**クリーン**（標準ライブラリ + pip のみ）である。サードパーティライブラリ（requests / numpy など）を使うには、まず `install_package` で pyruntime にその場でインストールする必要がある —— 開発版はプリインストール済み、パッケージ版は必要に応じてインストールする、という正常な差異である。
- `build.ps1` の末尾で `pip OK / 欠落` を表示する。欠落していれば `build.ps1` を再実行するか、手動で補う：embeddable Python を `dist\Star\pyruntime\` に解凍し、`._pth` を編集して `import site` を有効化し、`get-pip.py` を一度実行する。

### 2.4 初回パッケージでよくある調整（重依存アプリの通弊）

パッケージし終えたら、まず**ダブルクリックで一度走らせ、各機能を一通りクリックして**みて、エラーに応じて `star.spec` を補ってください。

- **OCR を使うとすぐクラッシュ / モデルが見つからない** → RapidOCR のモデルが収集しきれていない。spec はすでに `collect_data_files("rapidocr_onnxruntime")` 済みだが、それでも欠ける場合はエラーのパスに従い `datas` で `.onnx` を補う。
- **`ModuleNotFoundError`** → モジュール名を spec の `hiddenimports` に追加する。
- **ウィンドウが起動しない / Qt プラグインが欠落** → spec 冒頭の `collect_all("PySide6")` でカバー、またはコマンドラインで `--collect-all PySide6`。
- **`uiautomation` / `comtypes` のエラー**（画面の閲覧 / コントロールのクリック）→ spec はすでに `comtypes*` を含むが、それでもエラーが出れば `--collect-all comtypes` を追加。
- アイコンの追加：`.ico` のパスを `star.spec` の `icon=` に記入する。
- ウイルス対策ソフトが PyInstaller で作った exe を誤検出することがある（通弊。信頼に追加すればよい）。`build\`、`dist\` はすでに `.gitignore` 済み。

---

## 3. テストとトラブルシューティング

安全ガードレールにはユニットテストがあり（`tests/test_safety.py`、`uv run --no-dev --group test python -m pytest tests/ -q` で実行。`check_blocked` / `check_risky` の正例・負例と「すでに遮断済みなら二重に警告しない」優先順位をカバー）、それ以外に自動化された UI テストはなく、**手動ウォークスルーのチェックリスト**で項目ごとに検証します（操作 → 期待）。実行前に Star が起動済みで、コントロールパネルに API が設定済みであることを確認してください。観察する経路は 3 つ。

1. ペットのそばの**吹き出し / 黒板 / ポラロイド / 確認パネル**；
2. **コントロールパネル**（インターフェース / 対話 / 権限 / About の 4 ページ）；
3. **監査ログ** `data/logs/audit-YYYYMMDD.jsonl`（`Get-Content` で最新数行を確認）。

ウォークスルーのカバー範囲：基本的な対話と表情、コマンドアクション（perform / 小芝居）、アイドル行動、割り込み、自発メッセージ、祝日 / 誕生日の認識と交流統計、記憶とエピソードログ、グローバルホットキー（ついで書き換えを含む）、クリップボードとウィンドウ、コントロールパネル、リマインドとスケジュール（繰り返し / システム通知を含む）、定刻の画面見守り、タスク一覧パネル、サブエージェント編成、思考アニメーション、黒板と画像、振り返りのゲート制御；さらに伴生行動：投喂（ファイルのドラッグ / 保護パスのブロック / ドキュメントをナレッジベースへ）、ゴミ虫を潰して実際のクリーンアップ、遊び（ボール投げ / 窓辺の見張り / くすぐり / 墨の足あと）、マシン擬態・天気擬態、会議ミュート、儀式（朝の気分予報 / 記念日ケーキ / ポモドーロ / おやすみの手振り）、バックグラウンドタスク見守りとクリップボードのお返し、身体感覚の注入。

**よくあるトラブルシューティング**：

- **返答言語が正しくない** → コントロールパネル「対話」ページの「返答言語」欄（空 = あなたが話した言語に追従）。
- **一部の能力が「できない」** → 「権限」ページで対応する能力グループ（ネット接続 / 操作 / コマンド）をオフにすると、ツールがモデルのツール表から隠され、これは期待どおりの挙動。
- **自発メッセージが出ない** → 節制している：在席 + ビジーでない + rapport 到達 + クールダウン経過が必要。素早く検証したければ自発頻度を「おしゃべり」にする。それでも即時にはトリガーされない。
- **起動時に「ホットキーを取得できなかった」と表示** → `Ctrl+Alt+S` が他のソフトに占有されている。ペットをクリックすれば引き続きチャット可能。
- **純粋な雑談の後には振り返りのモデル呼び出しが発生しないはず**（短い雑談は振り返りをスキップ）。ツールを使った実質的なタスクのみが振り返りを行い、記憶 / ログ / 自己像を更新する可能性がある。

---

## 4. 能力と安全性

Star は**あなたのマシン上で任意のコマンドとコードを実行し、マウス・キーボードを操作し、ファイルを読み書きできます** —— これが強力さの源泉であり、同時にリスクも意味します。

- 本質的に**あなた本人と同等の PC 操作権限**を持つ。
- コントロールパネルで能力を**グループ単位でオフ**にできる（ネット接続 / 操作 / コマンド実行）。不安な場面では権限を下げられる。
- **不可逆 / 高リスクな操作は `confirm` パネルを経由する**。「実行 / 中止」をポップアップし、あなたがうなずいて初めて動き出す。
- **安全ガードレール**（`executor/safety.py`）は、**ごく小さく、高精度で、破滅的に不可逆な**操作（ディスクのフォーマット、`diskpart` / `remove-partition`、`reg delete HKLM /f`、生ドライブのルート / システムディレクトリへの再帰的強制削除など）のみをハードに阻止する。あえてサンドボックスは設けず、通常のファイル削除は阻止しない —— 檻に入れた助手ではなく、「フルアクセス」の相棒。
- **投喂 / ゴミ虫の削除境界**：ファイルをドラッグして Star に与えると、通常のファイル / ゴミは**ゴミ箱へ送る**（`FOF_ALLOWUNDO`、復元可能・完全削除ではない）。システムディレクトリ、`Program Files`、ホームディレクトリとその一階層のサブディレクトリ自身、ドライブルート、データディレクトリなどの**保護パス**は見抜いて拒否し、`.exe / .bat / .ps1 / .dll` などの実行ファイルも拒否する。200MB 超やディレクトリ丸ごとは**先に確認**を出す。ドキュメントはナレッジベースへ読み込むだけで**原本は削除しない**。ゴミ虫を潰すと、システム temp 内の**更新が 7 日以上前**の期限切れファイルを掃除する —— これは実削除（ゴミ箱経由ではない）で、使用中のファイルは自動でスキップする。
- API Key などの設定、記憶 / ナレッジベース / ログはすべて**ローカル保存**。「記憶を消去」ボタンでワンクリック消去できる（自己像を含み、出荷時の地色へ戻る）。

---

## 5. ローカルデータ

すべて `data/` の中にあります（開発時はプロジェクト内、パッケージ後は `%APPDATA%\Star` へ移動。`STAR_DATA_DIR` で上書き可能）。

| ファイル | 内容 |
| --- | --- |
| `settings.json` | インターフェース / モデル / 言語 / 能力スイッチ / 自発頻度 |
| `emotion.json` | valence / arousal / rapport + タイムスタンプ |
| `persona.json` | 自己像（人格進化レイヤー） |
| `stats.json` | 交流統計：初対面の時刻 + 累計インタラクション回数 + 投喂量 / 深夜付き合い日数 / 各儀式の重複防止マーカー |
| `usage.json` | token 使用量の計測（日ごとに入力 / 出力 / キャッシュヒットを累計） |
| `proactive.json` | 自発メッセージのクールダウン / カウント状態 |
| `reminders.json` | トリガー待ちのリマインド / スケジュールタスク（繰り返しルールを含む） |
| `journal.json` | エピソードログ（直近の数件） |
| `last_entrance.txt` | 前回の入場アニメの種類（起動のたびに前回と重複しないため） |
| `memory/memory.db` | 長期記憶（SQLite） |
| `docs.db` | ナレッジベースの chunk + 埋め込み |
| `skills/` | 自作スキルのコード + レジストリ |
| `logs/audit-*.jsonl` · `logs/crash.log` | 監査ログ（日次）+ クラッシュスタックダンプ |
| `mcp.json` | MCP コネクタの設定（任意） |
